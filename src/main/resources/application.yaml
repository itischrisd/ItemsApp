spring:
  application:
    name: ItemsApp
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:mem:items
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: false
    open-in-view: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
